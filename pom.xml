<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>net.rationalminds</groupId>
	<artifactId>RationalMassMailer</artifactId>
	<name>RationalMassMailer</name>
	<url>http://rationalminds.net</url>
	<version>2.0</version>
	<packaging>jar</packaging>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.release>25</maven.compiler.release>
		<javafx.version>25.0.4</javafx.version>
		<app.mainClass>net.rationalminds.massmailer.ui.MassSenderUI</app.mainClass>
		<jar.mainClass>net.rationalminds.massmailer.ui.Launcher</jar.mainClass>
		<win.icon>${project.basedir}/pluto-win-exe.ico</win.icon>
		<mac.icon>${project.basedir}/pluto-mac-app.icns</mac.icon>
	</properties>
	<dependencies>
		<dependency>
			<groupId>com.sun.mail</groupId>
			<artifactId>javax.mail</artifactId>
			<version>1.6.2</version>
		</dependency>

		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-base</artifactId>
			<version>${javafx.version}</version>
			<classifier>${javafx.platform}</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-graphics</artifactId>
			<version>${javafx.version}</version>
			<classifier>${javafx.platform}</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-controls</artifactId>
			<version>${javafx.version}</version>
			<classifier>${javafx.platform}</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-fxml</artifactId>
			<version>${javafx.version}</version>
			<classifier>${javafx.platform}</classifier>
		</dependency>
		<dependency>
			<groupId>org.openjfx</groupId>
			<artifactId>javafx-web</artifactId>
			<version>${javafx.version}</version>
			<classifier>${javafx.platform}</classifier>
		</dependency>
	</dependencies>
	<profiles>
		<profile>
			<id>mac-x86_64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>x86_64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>mac</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>mac-aarch64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>aarch64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>mac-aarch64</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>windows</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<properties>
				<javafx.platform>win</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>linux-x86_64</id>
			<activation>
				<os>
					<family>unix</family>
					<name>linux</name>
					<arch>amd64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>linux</javafx.platform>
			</properties>
		</profile>
		<profile>
			<id>linux-aarch64</id>
			<activation>
				<os>
					<family>unix</family>
					<name>linux</name>
					<arch>aarch64</arch>
				</os>
			</activation>
			<properties>
				<javafx.platform>linux-aarch64</javafx.platform>
			</properties>
		</profile>

		<!-- jpackage can only produce a .pkg when running on macOS itself
		     (it shells out to native Apple packaging tools), so this build
		     step only activates here. On Windows/Linux, `mvn package` still
		     produces the jar and the .exe, just not the .pkg. -->
		<profile>
			<id>mac-pkg</id>
			<activation>
				<os>
					<family>mac</family>
				</os>
			</activation>
			<build>
				<plugins>
					<!-- jpackage bundles its whole input directory into the app
					     image, so give it a clean folder containing only the
					     shaded jar rather than all of target/. -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-resources-plugin</artifactId>
						<version>3.3.1</version>
						<executions>
							<execution>
								<id>copy-jpackage-input</id>
								<phase>package</phase>
								<goals>
									<goal>copy-resources</goal>
								</goals>
								<configuration>
									<outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
									<resources>
										<resource>
											<directory>${project.build.directory}</directory>
											<includes>
												<include>${project.artifactId}.jar</include>
											</includes>
										</resource>
									</resources>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<plugin>
						<groupId>org.panteleyev</groupId>
						<artifactId>jpackage-maven-plugin</artifactId>
						<version>1.8.0</version>
						<executions>
							<execution>
								<id>build-pkg</id>
								<phase>package</phase>
								<goals>
									<goal>jpackage</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<name>${project.name}</name>
							<appVersion>${project.version}</appVersion>
							<vendor>rationalminds.net</vendor>
							<destination>${project.build.directory}/installer</destination>
							<input>${project.build.directory}/jpackage-input</input>
							<mainJar>${project.artifactId}.jar</mainJar>
							<mainClass>${jar.mainClass}</mainClass>
							<icon>${mac.icon}</icon>
							<type>PKG</type>
						</configuration>
					</plugin>

					<!-- jpackage always names its output "<name>-<appVersion>.pkg";
					     drop the version suffix so it's just RationalMassMailer.pkg. -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>3.1.0</version>
						<executions>
							<execution>
								<id>rename-pkg</id>
								<phase>package</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<move file="${project.build.directory}/installer/${project.artifactId}-${project.version}.pkg"
											  tofile="${project.build.directory}/installer/${project.artifactId}.pkg" />
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.16.0</version>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
				<configuration>
					<!-- Keep the pre-shade thin jar out of target/'s root so target/
					     only ever holds the final shaded jar alongside the exe/pkg. -->
					<outputDirectory>${project.build.directory}/thin-jar</outputDirectory>
					<archive>
						<manifest>
							<mainClass>${jar.mainClass}</mainClass>
						</manifest>
						<manifestEntries>
							<url>${project.url}</url>
							<Built-By>${project.groupId}</Built-By>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.2</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<finalName>${project.artifactId}</finalName>
					<filters>
						<filter>
							<artifact>*:*</artifact>
							<excludes>
								<exclude>META-INF/*.SF</exclude>
								<exclude>META-INF/*.DSA</exclude>
								<exclude>META-INF/*.RSA</exclude>
							</excludes>
						</filter>
					</filters>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.openjfx</groupId>
				<artifactId>javafx-maven-plugin</artifactId>
				<version>0.0.8</version>
				<configuration>
					<mainClass>${app.mainClass}</mainClass>
				</configuration>
			</plugin>

			<!-- Wraps the shaded jar into a Windows .exe. Buildable on any OS
			     (launch4j ships prebuilt native stubs); running the result
			     still requires a Java 25+ runtime on the target Windows machine. -->
			<plugin>
				<groupId>com.akathist.maven.plugins.launch4j</groupId>
				<artifactId>launch4j-maven-plugin</artifactId>
				<version>2.7.0</version>
				<executions>
					<execution>
						<id>build-exe</id>
						<phase>package</phase>
						<goals>
							<goal>launch4j</goal>
						</goals>
						<configuration>
							<headerType>gui</headerType>
							<jar>${project.build.directory}/${project.artifactId}.jar</jar>
							<outfile>${project.build.directory}/${project.artifactId}.exe</outfile>
							<errTitle>${project.name}</errTitle>
							<icon>${win.icon}</icon>
							<singleInstance>
								<mutexName>rationalminds_mass_email_sender</mutexName>
								<windowTitle>Rational Mass Mailer</windowTitle>
							</singleInstance>
							<jre>
								<minVersion>25</minVersion>
							</jre>
							<versionInfo>
								<fileVersion>${project.version}.0.0</fileVersion>
								<txtFileVersion>${project.version}</txtFileVersion>
								<fileDescription>${project.name}</fileDescription>
								<copyright>rationalminds.net</copyright>
								<productVersion>${project.version}.0.0</productVersion>
								<txtProductVersion>${project.version}</txtProductVersion>
								<productName>${project.name}</productName>
								<companyName>rationalminds</companyName>
								<internalName>${project.artifactId}</internalName>
								<originalFilename>${project.artifactId}.exe</originalFilename>
							</versionInfo>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>